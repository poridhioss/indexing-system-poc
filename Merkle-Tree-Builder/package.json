{
  "name": "merkle-tree-builder",
  "version": "1.0.0",
  "description": "Merkle tree builder for efficient change detection in code indexing",
  "type": "module",
  "main": "dist/example.js",
  "types": "dist/example.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/example.js",
    "dev": "ts-node --esm src/example.ts",
    "clean": "rm -rf dist .puku",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "merkle-tree",
    "file-hashing",
    "change-detection",
    "code-indexing"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@parcel/watcher": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}